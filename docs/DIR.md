# 📂 Directory Structure

```text
/
├── app/
│   ├── check/                  # 통합 진단 페이지
│   ├── report/[id]/            # 결과 리포트 (지원사업 + 제품)
│   │   └── components/
│   │       ├── MinistryBadge.tsx  # 부처별 뱃지 (복지부/노동부...)
│   │       └── FormDownloader.tsx # 신청서 다운로드 버튼
│   ├── admin/                  # n8n 수집 제품 승인 대시보드
│   └── api/
│       ├── generate-form/      # 서류 생성 API (Proxy to Python)
│       └── webhook/n8n/        # n8n 데이터 수신용
├── lib/
│   ├── ministry-logic.ts       # 9개 사업 판별 하드코딩 로직 (1차 필터)
│   ├── form-templates/         # 서류 양식 정보
│   └── gemini.ts
├── supabase/
│   └── migrations/             # SQL 파일들
└── ...